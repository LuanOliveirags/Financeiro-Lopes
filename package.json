{
  "name": "wolfsource",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "WolfSource - Gestão Financeira",
  "scripts": {
    "copy": "node scripts/copy-web.js",
    "sync": "node scripts/copy-web.js && cap sync android",
    "open:android": "cap open android",
    "build": "node scripts/copy-web.js && cap sync android && cap open android",
    "icons:android": "node scripts/generate-android-icons.js"
  },
  "dependencies": {
    "@capacitor-firebase/messaging": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "firebase-admin": "^13.8.0",
    "sharp": "^0.34.5"
  }
}
